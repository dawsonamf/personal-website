# Spec 1 correctness review (reviewer 4, fable-2)

Spec: `docs/intents/2026-09-05-theme-engine-rewrite/spec-1-migration-engine-parity.md` (902 lines) reviewed against `main` @ `0f196d0`, the intent, Astro 7.3.1 / Playwright 1.61.1 docs and source, the npm registry, GitHub docs and the nodejs release schedule, on 2026-09-05. Line numbers in "spec" refer to the spec file; `file:line` otherwise refers to the repo.

Method note: third-party claims were checked by two subagents (Context7 + pinned source tags + `npm view` + curl); every codebase claim was checked by reading the file. Subagents ran on the orchestrator's model (the brief suggested opus; the standing rule is not to switch models silently).

---

## Findings (ordered by severity)

### Blockers

**C-01. The deploy workflow never populates `public/vendor/`, so every vendored asset 404s in production.**
- severity: blocker · confidence: high
- spec: line 52 (D5: `scripts/vendor.mjs` runs "on `prebuild`/`predev`", output gitignored), line 68 (D21: `npm ci` → `astro build`), line 694 (§10: `npm ci`, `npx astro build`), line 771/T2 (`build` = `astro build`).
- evidence: npm docs, "Pre & Post Scripts": pre/post scripts run only for `npm run <name>`; `npx astro build` invokes the binary directly and consults no `scripts` entry. `public/vendor/` is gitignored (D5), so the checkout in CI has nothing there.
- wrong: with `npx astro build` in `deploy.yml`, `dist/vendor/` is empty; jQuery, jQuery UI, gsap, AOS (js+css), vanilla-tilt, github-dark and Font Awesome (css + webfonts) all 404 on the live site while the local `npm run build` (§10 step 1) works, so nothing before cutover catches it. `npm run build:preview` has the same hole (npm hooks are per script name; `prebuild` does not fire for `build:preview`).
- correct statement: `deploy.yml` runs `npm run build` (or an explicit `node scripts/vendor.mjs` step before `astro build`); `build:preview` gets its own `prebuild:preview` or calls the vendor script inline.

**C-02. The normaliser has no allow-list step, so the DOM-equality check fails on every themed page, every post and every utility page by construction.**
- severity: blocker · confidence: high
- spec: lines 15-16 and 841 ("§15 … is what the parity harness allow-lists"), line 603 (definition 1: `expect(newLines).toEqual(oldLines)`), line 663 (step 2: "Nothing else is removed"), lines 478-480 (themed pages gain `link[rel=canonical]` + `meta[name=robots]`), lines 537-541 (posts gain description = excerpt, OG, canonical), lines 411-414 and 498-499 (utility pages gain `#tc-dock`, `#tc-scrim`, `PickerFab`).
- evidence: old side after settle has none of those nodes: `blog/post.html:7` is `<meta name="description" content="A blog post by Dawson Metzger-Fleetwood.">` and there is no OG/canonical; `theme-cycler.js:558-559` returns before `injectDom()` appends the dock on pages without `.tc-nav-item` (privacy, 404, lexchat); the old site emits no canonical/noindex on `?style=` pages.
- wrong: with the normaliser as written (drop scripts/noscript/modulepreload only), `newLines` ≠ `oldLines` for 15 skins × every page (canonical + noindex), 16 × 3 posts (description/OG/canonical), 16 × 3 utility pages (FAB + dock + scrim). No ticket's "harness green" can be met.
- correct statement: §9 must specify the allow-list as normaliser rules, per page type, applied to the new dump: strip `link[rel=canonical]`/`meta[name=robots]` on themed pages, `meta[name=description]`/`meta[property^=og:]`/`link[rel=canonical]` on posts, and `.tc-fab`, `#tc-dock`, `#tc-scrim` on utility pages (or strip dock/scrim on both sides everywhere and assert them separately).

**C-03. Normaliser step 6 rewrites old links to un-themed root paths, but the new build under a theme emits `/<theme>/…`; bare `#x` links have no rule at all.**
- severity: blocker · confidence: high
- spec: lines 669-673 (step 6: `blog/`, `./`, `../blog/` → `/blog/`; `post.html?id=p` → `/blog/p/`; only `/?style=x` → `href(<page>, x)`), lines 468-476 (D31/§6.1 rule 5: engine routes get `'/' + theme + path`), line 449 (preset rows use `href()`).
- evidence: `js/nav-config.js:9-14` emits `#about` … `#contact` on home (prefix `''`), `blog/` for the Blog link, `index.html#about`/`./` on subpages; `blog/blog-listing.js:127-131` emits `post.html?id=p`; `js/blog-data.js:44` emits `../blog/post.html?id=fly-on-my-laptop`. Under theme `x` the new side emits `/x/#about`, `/x/blog/`, `/x/blog/p/`.
- wrong: after mapping, old `/blog/` vs new `/x/blog/` differs on every nav item, card, rail card and CTA on 15 skins × every page. On the default theme the home nav's bare `#about` is not in the map, so it also differs from `href('/#about')` = `/#about`.
- correct statement: after mapping an old link to its canonical root form, apply `href(mapped, theme)` on the old side (theme = the theme under test); add bare `#x` → `/#x` to the map (or have Nav emit `#x` on home via rule 2). Also add `https://www.dawsonamf.com/` → `/` and `https://www.dawsonamf.com/resources/…` → `/resources/…` (privacy logo `privacy/index.html:25`, vcard `js/nav-config.js:54`), and the Boxicons CDN URL → `/vendor-static/boxicons/…` (it is not in the `scripts/vendor.mjs` map the step cites).

**C-04. The `?style=` shim produces `/<id>//` for the home page, the one legacy URL form the site emits today.**
- severity: blocker · confidence: high (that the string is wrong; medium on whether the host tolerates `//`)
- spec: line 434: `location.replace('/' + q + location.pathname + location.hash)`; line 507 (§6.4 row 1); intent §4.5 ("Old `/?style=<id>` links redirect").
- evidence: `js/theme-cycler.js:277` `window.location.href = '/?style=' + encodeURIComponent(id)` is the URL every existing share link has; on `/` `location.pathname` is `'/'`, so the target is `'/brutalist' + '/' + ''`… i.e. `/brutalist//`.
- wrong: the most common old link redirects to a double-slash URL; neither T3's "Done when" nor §10 step 7 tests `/?style=<id>`.
- correct statement: `location.replace('/' + q + (location.pathname === '/' ? '/' : location.pathname) + location.hash)` (and decide `/index.html?style=` and `post.html?id=p&style=x`, see C-27); add `/?style=brutalist` → `/brutalist/` to T3's "Done when" and §10 step 7.

### Major

**C-05. D4's "there is no pre-paint override today" is false; dropping it causes a palette flash on every navigation and reload whenever palette-toy state is active.**
- severity: major · confidence: high
- spec: line 51 (D4: "The palette toy keeps restoring after load as it does today (`theme-cycler.js:126-147`); there is no pre-paint override today and none is added"), line 58 (D11: toy state survives reload), line 447 (§5.5 session restore kept).
- evidence: `js/theme-bootstrap.js:727-742` reads `sessionStorage['dawson-theme-cycler']` and, when `saved.colors.length === 5`, replaces `colors` before the ramp is written at `:764-774`, all inside the blocking head script. `js/theme-cycler.js:748-754` (`restore()` then `applyColors()`) re-applies the same values after `DOMContentLoaded` (the cycler is `defer`, last in `<body>`).
- wrong: with build-time tokens/ramp and no pre-paint override, each page first paints the theme's base palette and then flips to the toy palette when the deferred cycler runs; today it paints the toy palette directly. Q12/D11 extend toy persistence to reloads, so the flash also hits every reload.
- correct statement: `ThemeRuntime` reproduces the bootstrap's saved-colours override before paint (read `dawson-theme-cycler`, recompute `--<role>` and the 100-property ramp via a client copy of `ramp.ts`, which the 404 already bundles); `theme-cycler.js:126-147` cites `isReload()` + `restore()`, not the override.

**C-06. "Theme links are last in `<head>` on every page type" and "keeps the skin sheet after github-dark on posts" are both wrong; the parity position on posts (and the 404) is mid-head.**
- severity: major · confidence: high
- spec: lines 407-410 (§5.4), line 492 (§6.2 "The Shell then appends ThemeRuntime and ThemeAssets"), line 663 (normaliser keeps head order), lines 437-440 (D27 apply "appends the links").
- evidence: `blog/post.html:17` is the bootstrap script and `:19` is `github-dark.min.css`; `document.head.appendChild` at `theme-bootstrap.js:720` runs while the parser is at line 17, so the three theme links land after `theme-cycler.css` and before `github-dark.min.css` (and before the highlight/marked/mermaid scripts). `404.html:16` bootstrap, `:18-40` inline `<style>`: links land before the `<style>`. Home/listing/privacy have only `<script>` tags after the bootstrap, so "last" holds there after step 2 drops scripts. No cascade dependency exists: `css/themes/theme-base.css:13` (`[data-style] .blog-post-content pre`) and skin rules all carry `[data-style]`, out-specifying github-dark's single-class rules.
- wrong: `ThemeAssets` last on posts and `apply.ts` appending at the end of head on the 404 fail the head-order DOM diff on 15 skins × 3 posts × 2 viewports and 15 × 404.
- correct statement: `ThemeAssets` is emitted at the bootstrap's position (immediately after `/css/theme-cycler.css`, i.e. before `github-dark.min.css` on posts and before the inline `<style>` on the 404); `apply.ts` inserts before the 404's `<style>`, not at the end of `<head>`.

**C-07. The head's inline `mermaid.initialize` block cannot stay as-is once mermaid loads lazily.**
- severity: major · confidence: high
- spec: line 491 (§6.2 "the mermaid `initialize` block stays `is:inline`"), lines 548-553 (§7: mermaid "lazily injecting … only when `.mermaid` exists"; "The mermaid `initialize` block stays `is:inline`").
- evidence: `blog/post.html:22` loads mermaid synchronously and `:40` calls `mermaid.initialize(...)` synchronously in the same `<head>`.
- wrong: with the CDN script removed from `<head>`, the inline block throws `ReferenceError: mermaid is not defined` on every post; on `toolbelt` (matrix page, 2 diagrams: `blog/posts/toolbelt.md`) `mermaid.run` then renders with default theme colours instead of `--secondary`/`--text`/`--neutral-gray`.
- correct statement: `blog-post-client.js` calls `initialize` with the same variables and fallbacks after the lazily injected script loads and before `mermaid.run`; nothing mermaid-related stays in `<head>`.

**C-08. The masthead pick is the second `Math.random` draw on home, not the first.**
- severity: major · confidence: high
- spec: lines 632-636 (§9 determinism: `seedFor(index, n)` brute-forces "the smallest seed whose first draw gives … index"; "the masthead pick at `typing-engine.js:165` is the first draw on every page").
- evidence: `index.html:31` loads `jquery-3.6.0.min.js` with `defer`, before `typing-engine.js` (`:38`) and `script.js` (`:41`, which calls `startTypingSequence`); jQuery 3.6.0 executes `S.extend({expando:"jQuery"+(f+Math.random()).replace(/\D/g,""),…})` at load (verified in the CDN file, sha256 `ff1523fb…`). `js/typing-engine.js:165` is the only site-code draw; gsap's three references are inside functions. The listing loads no jQuery, so there the pick is the first draw.
- wrong: on home the brute-forced seed pins an arbitrary sequence, not index 3 (single-type, shortest settle) or index 0 (delete path). Both sides still agree, so screenshots match, but the settle-time and delete-path goals are silently missed and any assertion on the chosen index fails.
- correct statement: seed on the k-th draw per page (k = 2 on home, 1 on listing); e.g. mulberry32 seed 13 yields index 3 of 9 on the second draw (seed 18 on the first; §17.3's "seed 3 → index 6" verified).

**C-09. The masthead data model has no representation of the `callback` step (or the `pause` steps).**
- severity: major · confidence: high
- spec: line 243 (`steps: [ { type: … }, { delete: 14 }, { type: … }, … ]` "9 sequences verbatim"), line 160 (JSON island), line 576 (`script.js` "reads sequences from the JSON island"), line 580 (`typing-engine.js` verbatim).
- evidence: every sequence carries `{ action: 'callback', fn: startAnimations }` right after its first `type` step (`js/script.js:131-132, 145-146, 151-152, 155-156, 159-160, 166-167, 173-174, 180-181, 187-188`) and `{ action: 'pause', duration: 1500 | 1000 }`; listing likewise with `onBlogTypingComplete` (`blog/blog-listing.js:37-88`). `startAnimations` triggers the intro reveal.
- wrong: an implementer serialising the YAML shape shown drops the callback; the intro reveal never starts (harness would catch it, but the spec gives no rule).
- correct statement: the island carries `type`/`delete`/`pause` steps; `script.js`/`blog-listing-client.js` insert the page's callback after the first `type` step (today's invariant); `pause` durations are part of the verbatim data.

**C-10. §6.3's shared "utility layout (logo header, content, footer, no nav, as today)" is wrong for lexchat.**
- severity: major · confidence: high
- spec: lines 498-501.
- evidence: `lexchat/index.html` (19 lines) has only the `<iframe class="lexchat-iframe">`, no `.header-menu-container`, no logo, no footer, no Font Awesome/Boxicons; `404.html:45-67` and `privacy/index.html:23-127` do have logo + footer. Privacy's logo links to the absolute `https://www.dawsonamf.com/` (`:25`) while the 404's links to `/` (`:46`).
- wrong: a shared header+footer layout adds visible chrome to lexchat (parity break on 16 × 2).
- correct statement: lexchat renders the iframe only, plus the Shell's end-of-body mount and the FAB; the three utility pages share the Shell, not a header/footer layout.

**C-11. §11's "Spec 1 provides" column promises schema and Shell features that §4/§5 and the tickets never specify.**
- severity: major · confidence: medium
- spec: lines 736-752 (`fragments(n)`, per-slot `constraints`, optional `category`/`year`/`summary`/`description.s`/`group`/`images[]`, plural `one`/`other` template fields, a `chrome` slot, a `<defs>` slot, `--tc-z` "set by the theme") vs lines 266-283 (§4.1 rules 1-8: none of these), lines 400-414 (§5.4 Shell: no slots), line 771 (T2 delivers "schema (§4.1 rules 1-8)"), line 754 ("Nothing in the five themes requires a change …").
- evidence: `grep -rn tc-z css/ js/` → no such custom property exists today; no ticket lists the fields or slots.
- wrong: the desk-check (intent §8.1: "Desk-check the engine design against every demand … before building") is not backed by deliverables; an implementer following T2/T3 builds none of it, and cream then hits gaps the desk-check claimed were closed.
- correct statement: either add the optional fields, helpers and slots to §4.1/§5.4 and T2/T3, or change §11's column to "left to the cream spec" for each.

### Minor

**C-12. D18 silently deviates from intent §4.10 (locked: "`CNAME` and `.nojekyll` ride in `public/`").** confidence: high. spec line 65 gives reasons but never says it overrides a locked decision. The reasons hold (GitHub docs: custom workflows ignore any `CNAME`; `upload-pages-artifact@v5` `action.yml` tars with `--exclude=.[^/]*` unless `include-hidden-files: true`; Jekyll never runs on an Actions artifact), so no behavioral consequence. Correct: state the deviation explicitly.

**C-13. Q3's default contradicts intent §4.8, which names `/embedded-swift-agent/` as moving under `public/subsites/` with a redirect.** confidence: high. spec line 804. It is raised as a question, but the default assumed until answered should be the locked decision.

**C-14. `titleSuffix` "on every page" doubles the name on home and lexchat.** confidence: high. spec line 183; `index.html:6` is `<title>Dawson Metzger-Fleetwood</title>`, `lexchat/index.html:6` is `<title>LexChat</title>`; blog/post/privacy/404 carry ` | Dawson Metzger-Fleetwood`. Correct: suffix on blog, post, privacy, 404 only.

**C-15. Two YAML sketch lines are invalid as written.** confidence: high. spec lines 261-262: `updated: { xs: Updated January 10, 2026 }` and `message: { s: This page doesn't exist, or it moved. }` — a comma ends a plain scalar inside a flow mapping (`{ xs: 'Updated January 10', '2026': null }`, rejected by `strictObject`). Correct: quote any value containing `,`, `{`, `}`, `[`, `]`, `#` or `: `.

**C-16. §9 check 3 exact-equality of "every `--*` token on `<html>`" fails under marquee/doodle.** confidence: high. spec line 606 vs line 406 (`--prose-*` on the new side only) and line 674 (step 7 strips them for the DOM dump but check 3 does not). Correct: exclude `--prose-*` in check 3 too.

**C-17. HTML comment nodes are in `outerHTML` and are never dropped.** confidence: high. spec lines 660-668 (normaliser drops scripts/noscript/modulepreload only); `index.html:17,29,82,279`, `blog/index.html:69,84,89,101,106`, `blog/post.html:68-72`, `privacy/index.html:11,23`, `404.html:11-12` contain comments the components will not reproduce. Correct: drop comment nodes in step 2.

**C-18. D29's page scan fails on non-page files if it walks `dist/`.** confidence: medium. spec line 76 ("every emitted page has exactly one `#tc-dock` …"). The `/12years/` redirect stub (Astro's `redirectTemplate()` emits meta refresh + noindex + canonical only), `public/blog/post.html`, `public/subsites/**` and `public/embedded-swift-agent/index.html` have none of the engine elements. Astro's `astro:build:done` `pages[]` lists only `route.type === 'page'` routes (`core/build/generate.ts`), so the scan must iterate `pages`, not `dist/**/*.html`; say so.

**C-19. Wrong or off line citations (no behavioral consequence).** confidence: high.
- D4 `theme-cycler.js:126-147` → `restore()` is `:141-154`; `:126-131` is `isReload()`.
- D11 `theme-bootstrap.js:675-678` → that is reload detection; the wipes are `:687` (`dawson-style`) and `:730-731` (`dawson-theme-cycler`). D11/§8 `theme-cycler.js:276` → the navigation is `:277`.
- §5.4 `theme-bootstrap.js:646-669` → `:646-667` (`:669-670` are the storage keys).
- D16 `blog-post.js:4-14` → the `link` renderer is `:8-15` (`marked.use` spans `:4-30`).
- §6.2 "five OG/Twitter tags" → six (`index.html:11-16`: og:type, og:title, og:description, og:url, og:image, twitter:card).
- §9 settle step 2 "its 14 links" → `loadAllFonts` (`theme-cycler.js:370-386`) skips hrefs already in `<head>`, so 13 on a themed page with fonts (14 only on default/grid); the check is "stable", so harmless.

**C-20. §3.1 omits `harness/scripts.ts`, which §9 check 5 relies on.** confidence: high. spec line 106 vs line 613.

**C-21. Ticket path scoping contradictions.** confidence: high. T5 delivers "chart workflow paths" (line 774) but `.github/workflows/*` is only in T7's paths (line 788); T6 delivers the `redirects` config (line 775) but `astro.config.mjs` is only in T3's paths (line 783); no ticket moves `css/`, `resources/`, `blog/*.css`, `privacy/privacy-styles.css`, `lexchat/lexchat-styles.css` into `public/` (D18 assumes they are there from T3 on).

**C-22. `gh workflow run` in `refresh-chart-data.yml` needs `GH_TOKEN`.** confidence: high. spec lines 68, 696-699. `gh` is preinstalled on `ubuntu-latest` but unauthenticated; the step needs `env: GH_TOKEN: ${{ github.token }}` in addition to `permissions: actions: write` (fine-grained permission for `POST …/actions/workflows/{id}/dispatches` is Actions: write, confirmed).

**C-23. `404.astro` "inline `<style>` as today" must be `<style is:inline>`.** confidence: high. spec line 500 vs line 493 ("No component `<style>` anywhere in the canonical family"). A bare `<style>` in `.astro` is scoped (`data-astro-cid-*` selectors) and, with `inlineStylesheets: 'never'`, emitted as an injected `<link>` before `</head>`, moving the `.nf-*` rules and failing the DOM diff on the 404.

**C-24. §4.1's "No `m` or `s` variants are written in this spec" contradicts the sketch.** confidence: high. spec lines 285-288 vs `home.contact.body: { m }` (line 210), `meta.home.description: { m }` (line 187) and the `s` entries the same sentence lists. Consequence only if a canonical component asks for `l` where the file has `m` (build fails as unwritten); say which size each canonical slot requests.

**C-25. `PickerFab` CSS has no home.** confidence: medium. spec lines 77, 386, 775: the canonical family may not carry component `<style>` (line 493) and `public/css/theme-cycler.css` is not in T6's paths (line 787). State where the FAB's rules live (presumably `theme-cycler.css`, and add it to T6).

**C-26. Themed 404: the "Back to the home page" link and the dock's preset rows drop the theme.** confidence: high. spec lines 437-440, 498-500; `404.html:52` links to `/`. Today `?style=` seeds `sessionStorage['dawson-style']` (`theme-bootstrap.js:692`) so the click keeps the theme; under `/brutalist/nope/` the runtime apply changes attributes only, and `href()` computed the links for the default theme at build. Not listed in §15; either rewrite the links in `apply.ts` or list it.

**C-27. `?style=` on the `post.html` shim is unspecified.** confidence: high. spec line 509: `public/blog/post.html` is a static file (no `ThemeRuntime`), so `/blog/post.html?id=p&style=x` (the form the harness itself uses for the old side, line 627) either drops `x` or must carry `?style=x` through to `/blog/p/` for the ThemeRuntime shim to finish the job. Say which.

**C-28. `switchStyle` and toy state.** confidence: medium. spec lines 58, 448-450: D11 says toy state "persists for the session"; today `switchStyle` clears it on every switch (`theme-cycler.js:275`). Say whether the clear-on-switch stays (verbatim) or goes.

**C-29. §10 step 4 may need one prior run of the dispatch-only workflow.** confidence: low. GitHub docs (`workflow_dispatch`): "Once a workflow has run at least once, you can dispatch it against any branch or tag via the GitHub API or GitHub CLI." Dispatching `deploy.yml` with `--ref astro` before it has ever run on `main` may be rejected; run the dispatch-only copy once on `main` first (harmless with `deploy: false`), or note the fallback.

**C-30. Trivia.** confidence: high. D5 "one 3.3 MB UMD file": `mermaid@11.15.0/dist/mermaid.min.js` (3,312,967 B) is an esbuild IIFE global build (`globalThis.mermaid`), not UMD. §5.1 `RESERVED` omits `12years` (a route the config emits). §17.6 says `library-migration.md` §1's headline lumps jquery-ui into the byte-identical set; §1 item 2 (line 13) lists five libraries without jquery-ui and item 3 (line 14) states the one-byte difference, so the erratum describes wording I could not find. D1/§14.1 "Astro supports even lines only": the install page says "Odd-numbered versions like v23 are not supported"; the support-policy page says odd versions can be supported and dropped in a minor; net effect (use 24) unchanged.

---

## Verified correct (load-bearing claims that held)

Codebase (`main` @ `0f196d0`):
- `theme-bootstrap.js`: `ORDER` at `:637` (16 ids in the stated order); the seven globals (`__THEME_CYCLER_ENABLED :8`, `__THEME_REGISTRY :639`, `__THEME_ORDER :640`, `__styleAllowsTilt :646`, `__styleTypingMode :654`, `__styleTypingDeleteMode :664`, `__ACTIVE_STYLE :700`); attributes `data-style`/`data-still`/`data-no-tilt` only for non-default (`:703-706`); tokens then ramp (`:707-711`, `:764-774`); `STEPS` = 19 alphas, 5 roles → 100 properties, `hsla()` with `toFixed(0)`; link append order fonts → `theme-base.css` → skin, `data-style-asset="1"` (`:715-721`); default gets ramp only. Registry counts: tokens 15/16 (not default), fonts 14/16 (not default, grid), random 3 (brutalist, marquee, studio), flags 14/16 (not default, miami-deco) with shapes `{tilt:false}`, `{still:true}`, `{tilt:false,still:true}`, typing 8/16 (`'word'`/`'letter'`), `typingDelete` marquee only, polarity `'dark'|'light'`, colours = text/bg/primary/secondary/accent; inactive sheets space/vapor/wanted/constructivist on disk, no entry; labels Default / Swiss Grid / Porcelain / Street Poster.
- `theme-cycler.js`: `MEASURE 940 :596`, `EDGE 10 :597`, 300 ms close (`:706, :723`), 440 ms hide (`:676`), `void dock.offsetWidth :647`, hover gate `:594`, Space-to-shuffle `:768`, `dawson:palette` with no detail `:191`, `loadAllFonts :370` after idle `:762`, `dawson-theme-cycler`/`dawson-style` keys `:124-125`, `isReload() :126-131` and wipe `:748-750`, `.tc-nav-item` early return `:558-559`, `injectDom :507`, "all 20 skins" comment `:509`, `'Theme controls' :514`, `Lock/Unlock <Role>` `:462`, preset rows are `<a href="/?style=<id>">` (`:303`), dock appended then scrim (`:565-569`), `switchStyle` lands on `/?style=` (`:277`) and clears toy state (`:275`); entry fields the cycler reads (`colors`, `fonts`, `label`, `tokens`, `random`) are all in the §5.4 blob.
- `index.html`: head order `:1-27` as §6.2 (charset, viewport, title, description, canonical, 2 favicons, OG/Twitter, FA/Boxicons/AOS/Calendly CSS, styles, mobile, carousel, cycler, bootstrap); script attributes (gsap/jquery/jquery-ui/aos `defer`, vanilla-tilt sync, Calendly `async`, blog-data/featured-carousel/typing-engine `defer`, anim-utils sync, nav-config/script/cursor-follow `defer`, cycler `defer` at `:316`); `sec-num` 01-05 (`:97,144,218,243,264`); hero subtitle with `&nbsp;|&nbsp;` at `:79`; `calendly-link` at `:275`; `<br><br>` in about (3) and skills body 3 (1); aria-labels "Main navigation"×2, "Quick links", "Social links"×2; alts "Dawson Metzger-Fleetwood", "Map"; footer credit twice (`:305,312`); section/card titles (About Me, Skills, Web / iOS and visionOS / ML and RL, Where I've Worked, Selected Works, Blog, Contact, See all posts); job title = role ` @ ` company(+link); email `dawsonamf@icloud.com`.
- `blog/index.html`: `blog-listing-styles.css` before `theme-cycler.css` (`:25-26`); aos/vanilla-tilt/blog-data/featured-carousel/typing-engine/anim-utils sync, nav-config/cursor-follow `defer`, `blog-listing.js` sync then cycler `defer` (`:125-126`); 3 intro paragraphs; `sec-num` 01/02 (Selected Works / All Posts); title "Blog | …".
- `blog/post.html`: no canonical/OG/Calendly/AOS; description "A blog post by Dawson Metzger-Fleetwood."; DOM `:87-93` exactly as §7; mermaid init reads `--secondary`/`--text`/`--neutral-gray` with `#2c2c2c`/`#e6f1ff`/`#a2a2a3`; `blog-post.js` sync then cycler `defer`.
- `blog/blog-post.js`: renderer overrides `:4-30` (link http(s) → `text-link` + `_blank` + `noopener noreferrer`, else `text-link`; image → `img.blog-image`; code → `div.mermaid` / `pre>code.hljs.language-x` via `hljs.highlight` / `highlightAuto`); frontmatter line-splitter `:32-48`; `new Date(dateStr + ' 1')`; JSON-LD `:56-89` with the listed fields, description/keywords only when the post is in `BLOG_POSTS` (so gemma4 lacks them, §15.4); `document.title` set at runtime; pills = date, tags, `span#read-time`; copy buttons `.has-copy-btn` + `button.code-copy-btn` + `aria-label="Copy code"`; scripts chained sequentially, styles `forEach`; `mermaid.run` unconditional; read time `Math.max(1, Math.round(words/200))` from `innerText`; tilt on `.blog-image` gated on `__styleAllowsTilt`.
- `blog/blog-listing.js`: 7 sequences `:37-88` (terminal "Blog."); card markup `:127-139` with `data-aos-delay="${i*50}"`; no "All" pill; `filtered-out`; `AOS.init({ offset: 50 })` + `AOS.refresh()` on resize; tilt gated.
- `js/script.js`: 9 active sequences `:129-192` + 1 commented; index 3 is the single-type step; sequences 5-8 are two duplicate pairs ("builder." 4 of 9); `AOS.init()` `:212`; `renderBlogCards` `:384`; no `Math.random`.
- `js/featured-carousel.js`: `--ticker-run`/`--ticker-dur` set on `<html>` at `:413-414` (§17.1), `TICKER_SEP '✷'`, `TICKER_PASSES_PER_HALF 2`, `TICKER_CHARS_PER_SEC 402/46`, half doubled; dots built at load with `aria-label="Go to slide N"` (`:266`); wheel guard.
- `js/nav-config.js`: labels About/Experience/Projects/Blog/Contact/Resume/Email/Theme; `SCROLL_THRESHOLD 300`; Calendly URL from `--bg`/`--text`/`--primary` with fallbacks `1d1d1d`/`e6f1ff`/`61ffda`; six socials with the stated ids/labels/icons; theme trigger is `li.tc-nav-item > button.tc-nav-trigger`.
- `js/blog-data.js`: 8 projects, 7 descriptions with `<br><br>` (deep-rl none); silicon-fly tech and CTAs ("Read the post" → `../blog/post.html?id=fly-on-my-laptop`, "View on GitHub"); `BLOG_POSTS` 10 active + gemma4 commented at `:122-129` (§17.5); helm listing "…Minimalist Workspace Switcher for your IDE"/April 2026 vs post file "…for VS Code and Cursor"/March 2026; metr-doubling January 2026 vs February 2026 (Q1); autoencoders-1/2 external to aboutobjects.com, `AI & ML`; fly-on-my-laptop August 2026; embedded-swift-agent project CTA `/embedded-swift-agent/`.
- `js/typing-engine.js:165` is the only site-code `Math.random`; `anim-utils.js` `animationend` → `animation: none` pin (`:15-21`), ~10 pinned elements on home; `--section-rule` and `#highlight` exist.
- CSS: `marquee.css:572` `content: var(--ticker-run, "<unit ×12>")` (unit repeats exactly 12 times); `doodle.css:535` `content: 'currently here \2713'`; the only other word literal is blueprint's `"FIG. "` counter prefix (`:652`); `.tc-toggle` in 16 skin sheets + `theme-base.css:81`; `theme-base.css` has no `.hljs` rule.
- Posts: 11 files; `../../resources/` in six (arena-freshness, autoencoders-1/2, college-projects, embedded-swift-agent, helm); `post.html?id=` in autoencoders-1 ↔ 2; `scripts`/`styles` frontmatter in three (gemma4, metr-doubling, underviewed-art), Plotly 2.27.0 on two, js-yaml 4.2.0 on one, asset paths `posts/assets/…`; toolbelt 2 mermaid + bash + json; embedded-swift-agent 9 swift + 1 c + 1 image; only toolbelt has mermaid; privacy body ≈ 740 words, "Updated January 10, 2026", 8 `<h2>`; 404 strings, `.nf-*` inline style, noindex meta.
- `sitemap.xml`: `color-randomizer` entry (no such post); exactly three `lastmod` disagree with post dates (helm, embedded-swift-agent, metr-doubling); `robots.txt` has the `Sitemap:` line.
- Assets/subsites/workflow: `cohorts-chart.js:642-682` and `job-market-chart.js:12` use page-relative `posts/assets/…` (§17.7); `embedded-swift-agent/agent.js:11-18` bare `+esm` CDN import, 4 files; `12years/` = `index.html` + `then.jpeg` + `now.jpeg`, gsap 3.12.5 + ScrollTrigger from cdnjs; `.github/workflows/refresh-chart-data.yml` commits with the default token, `permissions: contents: write`, paths `blog/posts/assets/*`; `docs/prebake-cohort-data.py:27` `OUT_DIR`; `docs/planned-posts/` has 3 `.md` drafts; `CNAME` and `.nojekyll` at the repo root; `docs/theme-explorations.html` has no `<script src>` (§17.9) and describes the `.tc-toggle` FAB (`:63`).
- Local machine: Python 3.9.6 (`--bind`, `--directory` present), Node v25.9.0, `~/Library/Caches/ms-playwright` has `chromium-1228`, `chromium_headless_shell-1228`, `ffmpeg-1011`.
- mulberry32: seed 3 → 0.7202 → index 6 of 9 (§17.3).

Third-party (docs/source/registry on 2026-09-05):
- `astro@7.3.1` is `latest` (published 2026-09-03); `engines.node >=22.12.0`, no upper bound; `compressHTML` default `'jsx'` since v7, `false` "preserves all whitespace", `true` lossless mode; `prerenderConflictBehavior` (`'error'|'warn'|'ignore'`, default `'warn'`, since v6.0); `build.inlineStylesheets 'never'`, `build.format 'directory'`, `trailingSlash 'always'`, `site`; static `redirects` emit `<meta http-equiv="refresh">` (+ noindex + canonical) with no status; `file()` `parser` yields one entry from one YAML document, and `file.ts:62-69` catches parse errors, logs, returns (empty on a cold cache; stale entries persist on a warm cache); `glob()` `generateId`, `retainBody` (default `true`, since 5.17); `{ params: { path: undefined } }` matches the root, and `segment.ts`/`pattern.ts`/`generator.ts` permit a standalone rest segment in a directory with nested static children; `astro:build:done` gets `{ pages, dir, assets, logger }` (`routes` removed in v6), a throw propagates to `throwAndExit` → `process.exit(1)`, `pages[]` holds only `type === 'page'` routes; `astro:config:setup` runs arbitrary code; `is:inline`, `<Fragment set:html>`; config cannot read `.env` via `import.meta.env`, `process.env` works; Sätteri (`@astrojs/markdown-satteri@0.4.0`) is the default pipeline, `@astrojs/markdown-remark` not installed (§17.2); `@astrojs/compiler-rs ^0.4.0`, unclosed tags error; `public/` copied untouched; nothing injected into `<head>` when there is nothing to inject, and injection lands immediately before `</head>`; `@astrojs/sitemap@3.7.4` is latest, `filter` gets the absolute URL, `serialize` may return `undefined`, 404/500 and redirects excluded by default; astro depends on `js-yaml ^4.3.0` (4.3.0 satisfies it; latest 5.4.1).
- `@playwright/test@1.61.1` exists, latest 1.63.0; 1.61.1 pins chromium + headless-shell build 1228, 1.63.0 pins 1243; browser GC on `npx playwright install`; `toHaveScreenshot` `maxDiffPixelRatio`/`threshold`/`animations`/`caret`/`scale`/`mask`; `webServer` array with `gracefulShutdown { signal, timeout }`; `snapshotPathTemplate` `{platform}`; `[['html',{open:'never'}],['list']]`; `page.clock` (1.45+); `emulateMedia({ reducedMotion })`; `addInitScript` before page scripts on every navigation; `route.abort`; `--grep`.
- GitHub: latest `actions/checkout v7.0.1`, `setup-node v7.0.0`, `upload-pages-artifact v5.0.0`, `deploy-pages v5.0.1`; `upload-pages-artifact@v5` drops dotfiles via its own `tar --exclude=.[^/]*` unless `include-hidden-files: true`; custom workflows ignore `CNAME`; `.nojekyll` only matters for branch publishing; `GITHUB_TOKEN` events never trigger `push` workflows, `workflow_dispatch`/`repository_dispatch` are the exceptions; `workflow_dispatch` requires the file on the default branch; dispatch endpoint needs Actions: write; `PUT /repos/{o}/{r}/pages` accepts `build_type legacy|workflow`, `source` optional, `https_enforced`; `404.html` at the publishing-source root is served for any missing path.
- Node: v25 EOL 2026-06-01, v24 Active LTS → Maintenance 2026-10-20 (`nodejs/Release schedule.json`); type stripping stable in 24.12, enabled by default since 23.6 (erasable syntax only).
- npm: every pinned version in §14.2 exists (`@astrojs/sitemap@3.7.4`, `js-yaml@4.3.0`, `marked@18.0.5`, `highlight.js@11.9.0`, `jquery@3.6.0`, `jquery-ui-dist@1.12.1`, `aos@2.3.1`, `vanilla-tilt@1.7.0`, `gsap@3.9.1`, `gsap@3.15.0`, `@fortawesome/fontawesome-free@6.5.1`, `mermaid@11.15.0`, `boxicons@2.0.9`); mermaid unpacked 76,342,428 B; boxicons has six runtime deps incl. `react ^16`, `react-dom ^16`, `react-router-dom ^4`; all vendor-map file paths exist (`aos/dist/aos.{js,css}`, `@fortawesome/fontawesome-free/{css/all.min.css,webfonts/*}` 8 files, …); `highlight.js@11.9.0/lib/common.js` registers 36 languages and the cdnjs `highlight.min.js` registers the identical 36; sha256 CDN = npm for jquery 3.6.0, gsap 3.9.1, vanilla-tilt 1.7.0, `github-dark.min.css`, FA `all.min.css`, aos.js; jquery-ui 1.12.1 differs by exactly one inserted byte (`\/` at offset 45840).

## Not verified
- The "~395 words" of `12years/index.html` prose (the page is 1,000+ lines of script; not worth extracting) and "738 words" for privacy (my crude count is ≈744 including headings).
- Exact bullet counts per job in `index.html` (the YAML sketch shows six for about-objects; a rough count gives seven `<li>` in the job-1 block); no consequence, T2 migrates verbatim.
- Cream's "~112 words per job" (research claim; not a spec-1 behavior).
- Whether GitHub Pages serves or 404s a `//` path (C-04 stands on the string either way).
- `ThemeRuntime` "~7 KB" (nothing to measure yet).
- That D27's runtime apply and the 404's bundled script produce zero head injection beyond the module script/modulepreload the normaliser drops (T0(b) covers; Astro source says nothing else is injected).
- Whether `gh workflow run --ref astro` is accepted before the workflow's first run (C-29, doc wording only).
- The research files' internal accuracy beyond the sections the spec cites.
