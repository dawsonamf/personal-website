# Spec 1 fix list (orchestrator aggregation, Fable 5.1, 2026-09-05)

Inputs: `02-architecture-fable.md` (candidates A1-A8), `03-thermonuclear-fable.md` (F1-F13), `04-correctness-fable-1.md` (C1-C24, T1-T6), `04-correctness-fable-2.md` (C-01..C-30), `04-correctness-opus-1.md` (C-01..C-50), `04-correctness-opus-2.md` (C1-C14 + minors), plus the orchestrator's own spot-checks (`orchestrator-notes.md`). Source tags below name the reviewer and finding id so the fix pass can pull the evidence from the raw report. Where reviewers disagreed, the orchestrator's spot-check decided (noted inline).

Rules for the fix pass: every item below is either **FIX** (change the spec) or **ACCEPT** (leave the design, add the stated sentence). Nothing here reopens an intent §4 decision except where the spec silently deviated from one, in which case the deviation is stated, not reversed. Structural items are rewrites of the affected sections as one coherent design, not patches on patches.

## A. Parity harness cannot pass as written (blockers)

A1. **FIX** Normaliser rule 6 maps old link forms to un-themed root paths while the new side prefixes `/<theme>/`; DOM equality fails on all 15 skins on every page. Rule 6 becomes: map every old internal form to its canonical route, then apply `href(route, <theme under test>)` on the old side. Add the missing forms: bare `#x` on home → `/#x`; `index.html`, `../index.html`, `./` (logo) → `/`; `https://www.dawsonamf.com/` (home and privacy logo) → `/`; `https://www.dawsonamf.com/resources/contact.vcf` → `/resources/contact.vcf`; the Boxicons CDN URL → `/vendor-static/boxicons/…`. [opus-2 C2; fable-1 C1; fable-2 C-03]

A2. **FIX** §9 defines parity as exact DOM equality and has no mechanism for the §15 allow-list. Add a normaliser step 8: a per-page-type exception table applied to the new dump (themed pages: `link[rel=canonical]`, `meta[name=robots]`; posts: `meta[name=description]`, `meta[property^="og:"]`, `link[rel=canonical]`; utility pages: `.tc-fab`, `#tc-dock`, `#tc-scrim`; everywhere: the dock's `data-*` prose attributes, and any attribute this fix list adds to `<html>`), and define checks 1-3 as "equal after the exception table". Also drop HTML comment nodes in step 2 (the pages carry comments the components will not reproduce). T4/T5/T6/T8 "harness green" then means green under that table. [opus-2 C3; opus-1 C-02; fable-2 C-02, C-17]

A3. **FIX** Check 3 ("every `--*` token on `<html>`, exact") contradicts step 7 (strip `--prose-*`); exclude `--prose-*` (and whatever A/D items add) in check 3 as well. [opus-1 C-13; fable-2 C-16]

A4. **FIX** Determinism: jQuery 3.6.0 draws `Math.random` at load (`expando`) and is a deferred script ahead of `typing-engine.js` on home, so the masthead pick is draw 2 on home and draw 1 on the listing; `seedFor(index, n)` must target the k-th draw (`seedFor(index, n, draw)`), or the init script counts draws. Also: "first draw on every page" is false for posts with chart assets (no consequence; fix the wording). [fable-1 C4; fable-2 C-08; opus-1 C-32]

A5. **FIX** State "sticky nav after scroll 400→200" never yields `.menu-sticky` (class only while scrolling up with `scrollTop > 300`); use e.g. 800→500. [fable-1 C18]

A6. **FIX** Settle step 2 "its 14 links": `loadAllFonts` skips hrefs already in `<head>`; drop the count, keep "stable for 500 ms". [fable-1 C15; opus-1 C-35; fable-2 C-19]

A7. **FIX** T3 done-when "inline style byte-equal to today's post-load `<html>`": the cycler rewrites the ramp and appends derived neutrals post-load; capture the reference before `theme-cycler.js` boots. And state the real order of today's inline style: interleaved per role (`--text`, `--text5`…`--text95`, then `--bg`, …), `hsla(H,S%,L%,A%)` with no spaces and a percentage alpha. Normaliser rule 7 compares `<html style>` as a declaration map (sorted), not verbatim, so build-time additions (D2 below) cannot reorder it. [fable-1 C20; opus-1 C-33]

A8. **FIX** The harness never enters palette-toy state, so §15 item 2 has no coverage; add a matrix state (shuffle, navigate, assert first paint) or at minimum a manual-QA line "navigate and reload with an active palette". [opus-1 C-19]

A9. **FIX** §9 vs intent §4.9: "carousel scroll" became "dot 3 clicked", leaving the wheel guard untested; add a `mouse.wheel` state on the track and state the deviation. [fable-1 C22]

A10. **ACCEPT** + one sentence: the old side loads eleven libraries from live CDNs that are not in the `route.abort` list, so the old side needs network and check 4 depends on CDN availability; the abort list covers only non-deterministic APIs. [opus-2 not-verified]

## B. Deploy and cutover cannot work as written (blockers)

B1. **FIX** `npx astro build` in `deploy.yml` never runs the `prebuild` hook, so gitignored `public/vendor/` is empty in CI and every vendored asset 404s in production (`build:preview` has the same hole). Resolution chosen (also thermo F10, arch A8): commit `public/vendor/` (one-time `npm run vendor`, which is `scripts/vendor.mjs` run by hand when a pin changes), drop the `prebuild`/`predev` hooks and the sha256 list (git is the hash table; Boxicons is already committed), and fold `vendor-static/` into `public/vendor/`. One table (`scripts/vendor-map.mjs`: npm path, public path, CDN URL) is imported by the copy script, the normaliser (rule 6 CDN→vendor) and `harness/scripts.ts`. D5 and §8 rewritten accordingly. [opus-2 C1; fable-2 C-01; thermo F10; arch A8]

B2. **FIX** `@astrojs/sitemap` writes `sitemap-index.xml` + `sitemap-0.xml`, never `sitemap.xml`; `robots.txt`'s `Sitemap:` line must point at `/sitemap-index.xml`, D24/§7 say so, and §14 gains an owner action (resubmit the sitemap URL in Search Console); `/sitemap.xml` becomes a dead URL and §6.4 says so (no meta-refresh can redirect an XML fetch). [opus-2 C4]

B3. **FIX** §10 add/add merge conflict: the dispatch-only `deploy.yml` is committed to `main` (step 2) while T7 adds the full `deploy.yml` on `astro`, and `astro` forks before step 2. Resolution: T7 first merges `main` into `astro` and edits the file that arrived, so step 6 is a clean merge; say so in §10 and T7. [opus-1 C-11]

B4. **FIX** `gh workflow run` in `refresh-chart-data.yml` needs `env: GH_TOKEN: ${{ github.token }}` in addition to `permissions: actions: write`. [fable-2 C-22]

B5. **FIX** Pin the `deploy` input: `type: boolean, default: false`, the guard `if: github.event_name == 'push' || inputs.deploy` on the deploy job, and the dispatch command `gh workflow run deploy.yml --ref astro -F deploy=false` (typed). [opus-2 C14; opus-1 C-45]

B6. **FIX** Run the dispatch-only workflow once on `main` before the `--ref astro` dispatch (GitHub: "once a workflow has run at least once, you can dispatch it against any branch"); harmless with `deploy: false`. [fable-2 C-29]

B7. **FIX** Wording: `PUT /repos/{owner}/{repo}/pages` takes `build_type` with `source` optional (documented), so drop "undocumented / 422 fallback"; `actions: write` is documented for the dispatch endpoint (T7's check is a confirmation, not the only evidence); `workflow_dispatch` and `repository_dispatch` are both exceptions to the `GITHUB_TOKEN` rule; §10 step 4's "the artifact builds and uploads while Pages is still legacy" is an inference, label it unverified like the other T7 items. [fable-1 T4, T5; opus-1 C-30, C-31]

B8. **ACCEPT** + mitigation: §14 item 3 (commit `docs/intents/`) publishes the planning corpus at live `/docs/intents/…` URLs while Pages is still legacy (D22 says `docs/` is served today). Keep the owner's ask, add the consequence to §14 and the cheap interim mitigation: a `Disallow: /docs/` line in `robots.txt` on `main` now. [opus-1 C-18]

## C. False claims about the codebase and parity gaps (blockers and majors)

C1. **FIX** D4 says "there is no pre-paint override today and none is added". False: `js/theme-bootstrap.js:727-742` reads `sessionStorage['dawson-theme-cycler']` inside the blocking head script and writes the saved five colours into the ramp before first paint (the finalizer's verification agent got this wrong; orchestrator re-checked the file). With build-time ramps and D11/Q12 persistence, every navigation and reload with an active palette would paint the base palette then flip. Restore the pre-paint override: the one remaining `is:inline` pre-paint script reads the key and rewrites the five roles and the 100-property ramp before paint, using the same ramp function as D2 below. Fix the `theme-cycler.js:126-147` citation (`isReload()` is `:126-131`, `restore()` `:141-154`, boot `:748-754`). [opus-1 C-03; fable-1 C2; fable-2 C-05]

C2. **FIX** Head position of the theme links (D29, §5.4, §6.2, D27): the bootstrap `<script>` sits immediately after `/css/theme-cycler.css` on all six pages and `document.head.appendChild` runs inside that blocking script, so today's links land right there: before `github-dark.min.css` on posts and before the inline `<style>` on the 404, not "last in head". No cascade dependency exists (`theme-base.css` and skin rules carry `[data-style]`; no skin sheet uses `.hljs`). `ThemeAssets` is emitted at that position by each canonical layout's head; the 404's runtime apply inserts at the same position. D29 keeps the Shell as the owner of `<html>` and the body tail; the head position is a layout fact. (Opus-1's "verified correct" entry claiming ThemeAssets-last is the parity position is mistaken; the orchestrator checked `index.html:26-27`, `blog/post.html:16-19`, `theme-bootstrap.js:715-721`.) [arch A2; fable-2 C-06; finalizer D29]

C3. **FIX** §7 post DOM omits `.blog-post-title`, `.blog-post-meta`, `.blog-post-content` (20 of 20 skin sheets target `.blog-post-content`); the contract is `article.blog-post-container > header.blog-post-header > h1#post-title.blog-post-title + div#post-meta.blog-post-meta`, then `div#post-content.blog-post-content`. [opus-1 C-01]

C4. **FIX** `titleSuffix` "on every page" is wrong for home (`<title>Dawson Metzger-Fleetwood</title>`) and lexchat (`<title>LexChat</title>`); suffix on blog, post, privacy, 404 only. [opus-1 C-05; opus-2 C10; fable-1 C12; fable-2 C-14]

C5. **FIX** D30 picker trigger: the cycler binds click on `.tc-nav-trigger` inside `.tc-nav-item` (`theme-cycler.js:686-688`) and hover-open on the item only under `(hover: hover) and (pointer: fine)` (`:594`); a bare `.tc-nav-item.tc-fab` never opens on touch. Contract: a mount is `.tc-nav-item` containing `button.tc-nav-trigger` (aria attributes as `nav-config.js:98-105`); `PickerFab.astro`, `ThemePicker.astro` and the canonical Nav emit both. [opus-1 C-04; fable-1 C6]

C6. **FIX** 404 and lexchat load no icon font today; the dock's lock/caret glyphs and the FAB's palette icon are Font Awesome. Add `/vendor/fontawesome/css/all.min.css` to those two pages and list it under §15 item 3 (or use inline SVG for the FAB and the dock's two glyphs; choose one and say why). [fable-1 C8]

C7. **FIX** §8 "`nav-behavior.js` and `cursor-follow.js` are `defer` everywhere": both are absent from privacy, 404 and lexchat, which load only the bootstrap and the cycler; taken literally the spec adds a sticky header and a cursor follower to three pages. [opus-2 C9; opus-1 C-15; fable-1 C9]

C8. **FIX** `blog.intro` as one `l` field drops the ids `blog-sub-text`, `blog-sub-text-2`, `blog-sub-text-3` that the intro wave (`blog-listing.js:8-10`), `blog-listing-styles.css` and 15 skin sheets select (`[id^="blog-sub-text"]`); the listing renders three paragraphs carrying those ids (three prose fields, or one field split and stamped by `BlogListing.astro`; say which). [fable-1 C3]

C9. **FIX** Masthead data model (with D5 below): today's sequences carry a `callback` step after the first `type` and `pause` steps (1500 ms, one 1000 ms, 800 ms on the listing); the YAML shape and the JSON island as specified cannot carry the callback, so the intro reveal would never start. [fable-2 C-09]

C10. **FIX** D16: (a) the contact paragraph's `mailto:` link carries `target="_blank" rel="noopener noreferrer"` today (`index.html:273`), so the renderer needs a `mailto:` branch; (b) D16's premise that `.calendly-link` exists only at `index.html:275` is false: `nav-config.js:59` puts it on the rendered social anchor (about four per page on home), and the Calendly click handler binds every `.calendly-link`. [fable-1 C5; opus-2 C8]

C11. **FIX** D17/§4.1 `jobs[].id` slugs: the jobs panel keys off `job-N` (`index.html:153` `data-job`, panels `id="job-1"…`, `script.js:273-302`); the canonical Jobs component keeps `id="job-{i+1}"`/`data-job` in source order; the prose id is a content key only. [opus-1 C-08]

C12. **FIX** §6.3 "utility layout (logo header, content, footer)" is wrong for lexchat, which is the iframe and nothing else (no logo, footer, or icon fonts); privacy's logo links to the absolute `https://www.dawsonamf.com/`, the 404's to `/`. The three utility pages share the Shell, not a header/footer layout. [fable-2 C-10]

C13. **FIX** `404.astro`'s inline `<style>` must be `<style is:inline>` (a bare component style is scoped and, under `inlineStylesheets: 'never'`, extracted to an injected `<link>`); §6.2's "no component `<style>`" names this as the single exception. [opus-1 C-06; fable-2 C-23]

C14. **FIX** D7/§4.1: `file()` treats a non-array parser return as an id→data map, so a plain `yaml.load` yields one entry per top-level key and the single-`superRefine` guarantee is lost; the parser must return `[{ id: 'prose', ...yaml.load(text) }]` (or `{ prose: yaml.load(text) }`). The loader does swallow YAML syntax errors (logs and returns), so rule 7's own parse-and-throw stays. [opus-1 C-07; fable-1 T1; fable-1 third-party A7]

C15. **FIX** D10: a throw inside `astro:build:done` propagates (`integrations/hooks.ts` rethrows; `runHookBuildDone` awaits it) and `astro build` exits non-zero, so the `process.exit(1)` fallback is unnecessary; state the source-verified behavior, keep the T0 check as confirmation. [fable-1 third-party A9]

C16. **FIX** D2 wording: `compressHTML` is `boolean | 'jsx'`; `true` is lossless whitespace removal, not "a third, HTML-aware mode"; `prerenderConflictBehavior` is `'error' | 'warn' | 'ignore'` (default `'warn'`) and in 7.3.1 also governs duplicate content-entry ids from `file()`. [fable-1 T2, T3; opus-1 C-47]

C17. **FIX** `gemma4-heretic-ara`: rule 3 requires a `posts` entry, no approved excerpt/tags exist, new strings must be drafts, and D10 refuses drafts; meanwhile §15 item 4 says its JSON-LD "gains description and keywords". Resolve: its `excerpt` and `tags` are explicit `null` (rendered as nothing, no unwritten error), §15.4 drops the gemma4 clause, and Q2 additionally asks whether the owner wants to approve an excerpt and tags for it. Also: resolving Q1 changes the listing cards and home rail for `helm` and `metr-doubling` (both in the matrix) and D8 gives gemma4's page tag pills it lacks today; add both to §15. [opus-1 C-12; fable-1 C7]

C18. **FIX** `?style=` shim: on `/` the expression yields `/<id>//`; it also drops every other query parameter, and the `post.html` shim drops `?style=` (`/blog/post.html?id=p&style=x` is the harness's own old-side URL form). Pin: `location.replace('/' + q + (location.pathname === '/' ? '/' : location.pathname) + <rest of query> + location.hash)`; the `post.html` shim forwards `style` (`/blog/p/?style=x`, which the page shim then finishes); add `/?style=brutalist` → `/brutalist/` to T3's done-when and §10 step 7. Unknown ids stay ignored (as `theme-bootstrap.js:693-696`). [fable-2 C-04, C-27; fable-1 C21; opus-2 C12]

C19. **FIX** Absolute self-links: the logo (`index.html:57`, `privacy/index.html:25`: `https://www.dawsonamf.com/`) and the vCard (`nav-config.js:54`) pass through `href()` rule 1 unchanged and leave the theme. Decide: the logo becomes `href('/', theme)` (keeps the theme, consistent with D11; list under §15 item 1), the vCard becomes root-absolute `/resources/contact.vcf` (a static asset, passthrough); the normaliser maps the absolute forms (A1). [opus-2 C13; opus-1 C-44; fable-1 C14]

C20. **ACCEPT** + list: on the runtime-themed 404 the "Back to the home page" link and the dock rows point at default-theme URLs; list under §15 item 11 (the 404 is best-effort themed). [fable-2 C-26]

C21. **FIX** `switchStyle` clears toy state today (`theme-cycler.js:275`) while D11 says toy state persists for the session; state that clear-on-switch stays (a new theme has a new base palette). [fable-2 C-28]

C22. **FIX** T3 "build emits 240 pages" is impossible before T5 delivers the posts collection; T3's done-when is the non-post routes (16 × 4 + `404.html`), 240 lands in T5. [opus-2 C5]

C23. **FIX** T2 → T3 is a dependency (T3's dock strings, theme labels and rule-4 cross-check are T2 deliverables); §12 says they can run in parallel. Correct the ordering (see D12). [opus-1 C-10]

C24. **FIX** Post assets: `public/blog/posts/assets/*` ship visitor-facing strings (chart labels, `underviewed-art.js:295` builds an aria-label from a museum API field) with no prose home and no exemption. Posts stay Markdown outside `prose.yaml` by intent §4.6/§7, so post bodies and their per-post assets are already-approved content; add them to Q11's exemption list and the `CLAUDE.md` rule. [opus-2 C11]

## D. Structural findings adopted (architecture + thermonuclear)

D1. **FIX** Delete the runtime registry blob and the seven `window.__*` globals (thermo F1; supersedes arch A7 "defer"). Adopted because the blob is new machinery this spec invents, every consumer outside the cycler reads one flag of the active theme that `<html>` already carries, and the edits are output-identical (intent §4.13). Concretely: `<html>` gains `data-typing` and `data-typing-delete` beside `data-still`/`data-no-tilt` (added to the normaliser exception table and §15.10); `typing-engine.js:99,109` read `document.documentElement.dataset` with the same fallbacks; the five tilt gates read `!document.documentElement.hasAttribute('data-no-tilt')`; the build-rendered `#tc-presets` rows carry `data-id`, the `--tc-row-*` properties and `data-fonts`; the active theme's `polarity` and (for the three themes that have one) `random` profile ride on the active row (a small JSON attribute or one `<script type="application/json">` inside `#tc-dock`); the cycler reads rows and `<html>` (`dataset.style || 'default'`), snapshots the six neutral tokens from `<html style>` at boot (the bootstrap writes the entry's tokens before any toy override, so the values are the base values), and navigates to the clicked row's `href` (`__PAGE_PATH` deleted); `__THEME_CYCLER_ENABLED` deleted (the cycler runs wherever `#tc-dock` exists). The only `is:inline` pre-paint script is the palette-toy override (C1) plus, on default-theme routes, the `?style=` shim (C18), with `THEME_IDS` inlined at build; the full registry ships on exactly one page, the 404 (D27). §3.3's three bridges become one rule: a script reads `data-*` on the element it owns, or a JSON island the component that owns the data emits. Rewrite D4, D29's scan list, §3.3, §5.4, §5.5, §8 (typing-engine.js and the tilt-gate files are no longer verbatim: list the exact lines), `harness/scripts.ts`, §15. D26's future pre-paint block for mono becomes that theme's own inline component.

D2. **FIX** One pure projection from theme to `<html>` (arch A3 + thermo F2): `src/themes/apply.ts` exports `themeHtml(theme, colors = theme.colors) → { attrs, style, links }` (attribute list, ordered declarations: tokens then the interleaved per-role ramp then `--prose-*`, ordered link hrefs), no DOM and no other `src/` imports (`ramp.ts` is its only helper). The Shell spreads `attrs` and writes `style`; `ThemeAssets` maps `links`; the 404's script assigns the same value at runtime; the pre-paint toy override (C1) calls the ramp part with the saved colours. One byte-equality fixture (T3) covers all three adapters. The cycler's own copy of the ramp (`theme-cycler.js:24-40, 182-185`) stays until the cleanup pass; say so.

D3. **FIX** Decide a page's composition once (arch A1 + thermo F4): `src/layouts/compose.ts` returns one value for `(theme, pageType)` over the full page-type union including the utility pages: the layout, which of {fonts, `theme-base.css`, skin css} `ThemeAssets` emits, the picker mount (`'nav' | 'fab'`, with corner), canonical URL and robots flag. The Shell, `ThemeAssets` and `PickerFab` render that value and never inspect `theme.kind` or `theme.picker`; the Shell's `pageType` prop goes away. Each canonical layout (`Home`, `BlogListing`, `BlogPost`, `Privacy`, `NotFound`, `LexChat`) writes its own head (sharing one `Meta.astro` for the charset/viewport/title/favicon spine) so `Head.astro`'s per-page-type ladder disappears; `ThemeAssets` sits in each head at the C2 position. One table test over {skin, structural-with-layout, structural-fallback} × the six page types replaces the four-row prose table in §5.2. Fixes `PageType` lacking the utility pages (opus-1 C-36) and the FAB's two owners.

D4. **FIX** Build-time facts stay at build (thermo F3; arch A4): (a) carousel dots rendered in `FeaturedCarousel.astro` (`aria-label` from prose at build), client keeps click-centering and scroll tracking; (b) `--ticker-run`/`--ticker-dur` computed at build in the component (same formula: case-insensitive dedupe, `'✷ ' + t + ' '`, pass ×2 = half, run = half doubled, `Math.round(half.length / (402/46))` s) and emitted in `<html style>` after the ramp on pages with the carousel, `buildTickerRun` and `data-tech` deleted, the settle wait on `--ticker-run` deleted, D14's `--prose-ticker` kept for pages without the carousel; (c) copy buttons emitted by the marked `code` override (`pre.has-copy-btn > code + button.code-copy-btn[aria-label]`), the client delegates one click listener on `#post-content`, `addCopyButtons` and `data-copy-label` deleted; (d) mermaid: only posts whose rendered body contains `class="mermaid"` get the mermaid `<script>`, the `is:inline` initialize block (same three custom properties and fallbacks, same head position as today) and a `mermaid.run` call from the client; other posts get none of it, so no lazy loader exists and the initialize block can never run before the library (fixes fable-2 C-07). State the `<html style>` order argument for (b) and rely on A7's map comparison.

D5. **FIX** Masthead: prose stores each sequence as an ordered list of terminal strings plus an optional per-sequence `pause` override (the 1000 ms outlier at `script.js:146`; 800 ms on the listing); a ten-line pure function derives `type`/`delete`/`pause` steps by longest common prefix (verified: every hand-counted delete equals that derivation across all 16 sequences); the JSON island carries the derived steps; `script.js`/`blog-listing-client.js` splice their page's callback after the first `type` step. Schema rule 5 is deleted; T2's fixture proves the derived steps equal today's literal arrays. [thermo F5; arch A5; fable-2 C-09]

D6. **FIX** One door into prose (arch A6): the type of `prose.data` strips every size map (mapped type to `never`) so a sized field cannot be read except through `get`/`text`/`list`/`has` (`astro check` enforces it; one `@ts-expect-error` line in T2's test); chip and tag list items become `string | { draft }` so a new chip takes the draft flow. Replace the `{ paragraphs: 'br' }` option with `prose.paragraphs(path, size): string[]` and let the canonical component join with `<br><br>` (thermo F13).

D7. **FIX** `href()` rule 5 (thermo F7): "a root-absolute path is themed unless it is a static asset" (fixed prefix list: `/resources/`, `/vendor/`, `/subsites/`, `/embedded-swift-agent/` if it stays, `/blog/posts/`, `/css/`, `/js/`, `/blog/post.html`, `/sitemap-index.xml`, `/robots.txt`); no content import into `themes/paths.ts`, no `extraPages` knowledge; a mistyped internal path becomes a themed 404 that harness check 4 catches. `harness/urls.ts` derives its pairs from `THEME_IDS` × the page list through `href()` rather than a hand list; one `postIds()` helper replaces the three separate readers of the post id set.

D8. **FIX** Slim the structural surface (thermo F8; opus-2 C6, C7; fable-2 C-11): spec 1 ships `kind`, `layouts` (lazy imports), the fallback branch in `compose()` and its stub test. Everything else §11's "Spec 1 provides" column promised but nothing in §4/§5/§12 builds (`extraPages`, `picker`, `modes`, `chrome` slot, `<defs>` slot, `--tc-z`, `fragments(n)`, per-slot `constraints`, optional `category`/`year`/`summary`/`description.s`/`group`/`images[]`, plural template fields) moves to the right-hand column as "added by the first consumer that needs it, with the mechanism named" (theme-only pages are page files the theme spec adds under `src/pages/<theme>/`; no registry field). §11 remains the desk-check the intent asks for: it shows each demand lands without redesign. D26 and D28 updated; `PickerFab.astro` stays (D12).

D9. **FIX** Engine invariants out of the prose integration (thermo F9; fable-2 C-18): one `src/build/checks.ts` integration whose `astro:build:done` hook calls named pure functions (`assertNoUnwrittenSizes` reading the accessor's exported set from module scope, `assertShellInvariants`, `assertNoRelativeHrefs`, the YAML-syntax guard in `astro:config:setup`), iterating the hook's `pages[]` (page routes only; redirect stubs and `public/` files are not scanned), no `globalThis` side channel; T0 keeps one check that the accessor's module instance is shared.

D10. **FIX** `settle()` keeps the four generic waits; per-page-type readiness predicates live beside the sentinels in `harness/sentinels.ts` (thermo F6).

D11. **FIX** Dead code in files T4 edits is deleted in the same edit and listed in §8: `featured-carousel.js` `applyExpandVisualShell`/`EXPAND_VISUAL`; `theme-cycler.js` `STYLE_KEY`, `isReload`; `script.js` once-guards `heroChromeIn`/`subTextIn`; `typing-engine.js` `__restartTypingSequence`/`_lastConfig` (the file is edited under D1 anyway); `anim-utils.js` `data-style` MutationObserver stays (file untouched). [thermo F12]

D12. **FIX** Ticket graph (thermo F11; opus-1 C-10): T1 runs alongside T0 (needs only the baseline worktree and Playwright); T2 → T3 is sequential; T7 runs alongside T2/T3; T5 and T6 run in parallel after T4; T8 is the join. Fix path scoping: `.github/workflows/*` in T5's paths for the chart-workflow patch (or move that patch to T7 and say so); `astro.config.mjs` in T6's paths for `redirects` (or move the redirect config to T3); state which ticket moves `css/`, `resources/`, `blog/*.css`, `privacy/privacy-styles.css`, `lexchat/lexchat-styles.css` into `public/` (T3, since D18 assumes it from then on); `public/css/theme-cycler.css` in T6's paths for the FAB rules. [fable-2 C-21, C-25]

D13. **FIX** Render each post body once in `getStaticPaths` and pass the HTML as props (the `[...theme]/blog/[id].astro` route otherwise renders each post 16 times). [thermo, reviewed-no-card]

## E. Minor corrections (batch; each one line or one number)

E1. Line citations: D4/D11 `theme-cycler.js:126-147` → `:126-131` `isReload()`, `:132-141` `persist()`, `:141-154` `restore()`, boot `:748-754`; D11 `theme-bootstrap.js:675-678` is reload detection, the wipes are `:687` and `:730-731`; `theme-cycler.js:276` → `:277`; §5.4 `:646-669` → `:646-667`; D16 `blog-post.js:4-14` → `:8-15` and the attribute order is `href, class, target, rel, title?`; §5.4 `STEPS` is declared at `:671`; `parsePostDate` is `:50-54` and JSON-LD emits `description`/`keywords`/`datePublished` conditionally plus `@context`/`@type`; §4.1 `blog-listing.js:37-88` → `:38-88`. [opus-1 C-20..C-26; fable-1 C16; fable-2 C-19]
E2. §6.2: six OG/Twitter tags on home and listing (og:type, og:title, og:description, og:url, og:image, twitter:card), and the bootstrap `<script>` terminates today's head. [opus-1 C-09]
E3. `.tc-toggle` rules survive in 12 active + 4 inactive sheets plus `theme-base.css:81`; the FAB's `.tc-fab` inherits none of them. [fable-1 C10; opus-2 minor]
E4. D13: five sheets style `.privacy-*`/`.nf-*` (grid, banknote, gallery, neo-pop, plus inactive constructivist). [opus-1 C-27]
E5. D14: `blueprint.css:652` `"FIG. "` is a third word-bearing `content:` literal (a counter label, Q6 decoration); reword "only two". [opus-1 C-28]
E6. `picker.roles` keys are `text/bg/primary/secondary/accent` (`bg`, not `background`). [opus-1 C-29]
E7. `about-objects` has seven bullets (`index.html:163-176`), not six. [fable-1 C13]
E8. YAML sketch: quote flow-mapping values containing `,` (`updated`, `message`) and state the rule (quote `,{}[]#` and `: `). [fable-2 C-15]
E9. §4.1 "No `m` or `s` variants are written" contradicts the sketch (`home.contact.body.m`, `meta.*.description.m`); state which size each canonical slot requests. [opus-1 minor; fable-2 C-24]
E10. `RESERVED` adds `12years` (and any other redirect source). [opus-1 C-38; fable-2 C-30]
E11. §5.1: the four inactive skins' registry entries are commented out in place in `theme-bootstrap.js` and go with the file; note they survive in git history and `src/themes/README.md` points at the commit. [opus-1 C-39]
E12. §3.2 sitemap `serialize` cannot read a content collection from `astro.config.mjs`; it parses `prose.yaml` with js-yaml directly (or the build writes a small date map it reads). [opus-1 C-40]
E13. Storage keys: the canonical family keeps `dawson-theme-cycler` verbatim; only structural themes use the `theme.<id>.` namespace; say so beside D28. [opus-1 C-41]
E14. §15 item 8: highlight.js's runtime script disappears from post pages (rendering moved to build); §9 check 5's allow-list reflects it. [opus-1 C-42]
E15. `projects[].ctas`: reproduce the `linkTarget2` fallback (`embedded-swift-agent` has `external2` but no `external`; `deep-rl` has no CTA) or state the port changes CTA 2's `target`/`rel` on that card. [opus-1 C-49]
E16. T6: the path-segment 404 resolution is unit-testable (`[pathname.split('/')[1], q].find(...)`); add the test, keep the live check. [opus-1 C-50]
E17. D22/Q8: `docs/planned-posts/` holds three `.md` drafts plus `ai-job-market-listing.patch`. [opus-1 C-48]
E18. Q2 default: the autoencoders posts are not in today's sitemap, so "both in the sitemap (as today)" becomes "added to the sitemap". [fable-1 C24]
E19. Q3 default contradicts intent §4.8 (locked: `/embedded-swift-agent/` moves under `public/subsites/` with a redirect); the default assumed until the owner answers must be the locked decision (`/subsites/dawson/embedded-swift-agent/` + redirect); keep the question. Update §3.1, §6.4, §6.5, D7 prefix list, `RESERVED` accordingly. [fable-2 C-13; fable-1 C22]
E20. State each silent deviation from a locked decision explicitly, in the decision row and in §16: D5 vs intent §4.12 (mermaid, Plotly, js-yaml stay on CDN; files served verbatim rather than Vite-split), D18 vs intent §4.10 (`CNAME`/`.nojekyll` at the root; both facts verified), §9 vs intent §4.9 (A9). [opus-1 C-17; fable-1 C22; fable-2 C-12]
E21. `site.logo: { xs: D }` (the wordmark glyph on every page is prose). Add a `Skills` component to §3.1 and T4. `harness/scripts.ts` in the §3.1 tree. [opus-1 C-14, C-37, C-34]
E22. §7: `highlight.js/lib/common` registers 36 grammars = 34 languages + 2 sub-grammars (`php-template`, `python-repl`); the cdnjs bundle ships the same set. [fable-1 T6]
E23. D1 wording: Astro's install page says odd Node lines are not supported (not "only even supported"); `engines.node` has no upper bound. §14: Playwright's browser GC is refcounted by installed clients (1228 survives while 1.61.1 stays a dependency; `PLAYWRIGHT_SKIP_BROWSER_GC=1` exists); keep the caution, fix the mechanism. [fable-1 third-party A2; opus-1 C-46]
E24. D5 trivia: `mermaid.min.js` is an IIFE global build, not UMD. §17 item 6: re-check the wording against `library-migration.md` §1 items 2-3 and correct or drop the erratum. [fable-2 C-30]
E25. `apply.ts` on the 404 must also stamp what the cycler reads for the active theme (under D1: `data-style` and the active row marker), since the page is rendered as default. [fable-1 C19]
E26. §14 item 2 stays; add the Search Console resubmission (B2) and the `robots.txt` interim line (B8) as owner actions.

## F. Dispositions on findings not adopted

F1. Arch A7 (delete the flag helpers later): superseded by D1, adopted now.
F2. Opus-1's verified-correct claim that "ThemeAssets last" is the parity position: rejected on the orchestrator's own check (C2).
F3. Finalizer verification agent A's "no pre-paint override exists today": rejected on the orchestrator's own check of `theme-bootstrap.js:727-742` (C1); §17 gains a line saying the finalized draft briefly carried this error.
F4. Opus-1 C-18 (planning corpus published): accepted with mitigation (B8), not a change to the owner's ask.
F5. Fable-2 C-26 (themed 404 links): accepted and listed (C20).
F6. Thermo F8's reading that `PickerFab` is speculative: rejected; D12 needs it for the utility pages. The rest of F8 adopted (D8).
F7. Opus-2's suggestion to lint word budgets: not raised as a finding; D10 keeps budgets documented, not linted.
